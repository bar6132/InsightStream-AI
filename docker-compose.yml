version: '3.8'

services:
  backend:
    build: ./backend
    container_name: insightstream-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - DEPLOYMENT_MODE=local
    restart: always